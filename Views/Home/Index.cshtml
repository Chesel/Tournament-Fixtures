@{
    ViewBag.Title = "Home Page";
}

<div class="text-center mt-4">
    <a class="btn btn-primary btn-lg" href="@Url.Action("GenderManagement", "Home")">
        Manage Genders
    </a>
</div>
<div class="text-center mt-4">
    <a class="btn btn-outline-primary" href="@Url.Action("TournamentManagement", "Home")">
        Manage Tournaments
    </a>
</div>
<div class="text-center mt-4">
    <a class="btn btn-outline-primary" href="@Url.Action("DistrictManagement", "Home")">
        Manage Districts
    </a>
</div>
